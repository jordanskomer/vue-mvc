/**
 * To use in your components:
 * 
 * import globalMixins from 'mixins/global'
 * export default {
 *  mixins: [globalMixins]
 * }
 */

var globalMixins = {
  created: {
  },

  computed: {
  },

  methods: {
  }
}